# Environment Variables for Poly PNL

# ============================================
# REQUIRED FOR PRODUCTION (Rate Limiting)
# ============================================
# Upstash Redis credentials for rate limiting
# Get these from: https://console.upstash.com/
# Without these, rate limiting will be disabled (requests allowed with warnings)
UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_redis_token_here

# ============================================
# RECOMMENDED FOR PRODUCTION (CORS)
# ============================================
# Allowed origin for CORS (image proxy endpoint)
# Without this, CORS defaults to '*' (allows all origins - less secure)
# Example: https://yourdomain.com
ALLOWED_ORIGIN=https://yourdomain.com

# ============================================
# OPTIONAL
# ============================================
# Node environment (automatically set by deployment platforms)
# Options: 'development' | 'production'
NODE_ENV=production
